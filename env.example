# API Configuration
# Backend API URL (required for production)
NEXT_PUBLIC_API_URL=https://restaurant-menu-backend-a9f8.onrender.com/api/v1

# POS Printer Service (Optional - only needed if using local printer service)
# These are typically only used in local development or on cashier PCs
NEXT_PUBLIC_POS_SERVICE_URL=http://localhost:7777
NEXT_PUBLIC_POS_PRINT_KEY=pos-printer-secret-key-2024

